# cryptography 

# be a boss: brute force.

def encrypt(data):
    # no error checking cuz we ready 2 EAT baby.
    keyfile = open("basic_num_key.key")
    key = int(keyfile.readline())

    if (int(input()) not == key):
        


    